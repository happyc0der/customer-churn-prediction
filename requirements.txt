# Runtime dependencies for the Streamlit app (app.py + preprocessing.py).
# Version floors are the ones this project was last verified against.
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
pillow>=10.0
