# Runtime dependencies for the Streamlit app (app.py) and for retraining
# the model (train.py).
#
# scikit-learn 1.5 is the floor because the saved model is a
# TunedThresholdClassifierCV, which was added in that release.
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.5
scipy>=1.11
joblib>=1.3
pillow>=10.0
